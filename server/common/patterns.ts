export const DATE_FORMAT = new RegExp(/^\d{4}-\d{2}-\d{2}$/);
export const TAG_TYPES = new RegExp(/^(PATCH)|(ARTEFACT)$/);
export const EVENT_TYPE = new RegExp(/^((SM_DM)|(ANNIVERSARY)|(GENERAL))$/);
export const FOLDER_PATH = new RegExp(/^([\w-_]+\/?)*[\w-_]$/);